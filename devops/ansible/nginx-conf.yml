- hosts: all
  user: fmarinho
  become: yes
  tasks:
   - name: Applying nginx conf file
     template:
       src: /srv/templates/nginx.conf 
       dest: /etc/nginx/nginx.conf
       owner: root
       group: root
       mode: "u=rw,g=r,o=r" 
   -  service:
       name: nginx
       state: reloaded

- hosts: all
  user: fmarinho
  become: yes
  gather_facts: false
  tasks:
   - name: INSTALLING DOCKER
     yum:
       name: docker
       state: present
   - name: ENSURE THE DOCKER SERVICE IS RUNNING AFTER INSTALLATION
     service:
       name: docker
       state: started
